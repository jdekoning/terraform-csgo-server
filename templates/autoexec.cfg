hostname "${csgo-hostname}"
rcon_password "${rcon-password}"
sv_password "${sv-password}" //Only set this if you intend to have a private server!
sv_cheats 0 //This should always be set, so you know it's not on